<view class="contaner">
    <block wx:for="{{icons}}">
        <view>
            <icon type="{{item.type}}"
             class="icon"/> <text class="icon-text">{{item.text}}</text>
        </view>
    </block>
<view>